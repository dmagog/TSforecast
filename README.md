# TS Forecast

## Система прогноза ликвидности по филиалам сети металлопроката

Система прогноза ликвидности на основе данных отгрузок, оплат и оценки
кассовых разрывов.

---

## 🎯 Что описывает этот репозиторий

Проект состоит из двух частей:

- **Целевой (гипотетический) продукт** — описан в дизайн‑документе
  `docs/ml_system_design_doc.md` и отражает архитектуру production‑системы
  (Airflow, Postgres, MLflow, FastAPI и т.д.).
- **Демо‑референс для домашних заданий** — минимальный рабочий контур, который
  можно запустить локально: EDA‑ноутбуки, бейзлайны и MVP, а также batch‑инференс
  и FastAPI для проверки.

Ключевые разделы дизайн‑дока:

- `docs/ml_system_design_doc.md` — основной документ
- `docs/ml_system_design_doc.md#L380` — пилот (дизайн и критерии)
- `docs/ml_system_design_doc.md#L470` — внедрение (архитектура, SLA, нагрузка)

## 📌 Описание проекта

**TS Forecast** — это система прогнозирования финансовых потоков (отгрузки и
поступления оплат) по каждому филиалу производственно-торговой компании с
сетью **100+ подразделений**.

Основная задача системы — **снизить вероятность кассовых разрывов**, повысить
прозрачность финансового планирования и уменьшить трудозатраты филиалов на
ручные прогнозы.

---

## 🎯 Цели системы

- Прогнозировать **ежемесячные поступления средств** на основе данных об
  отгрузках, с учётом возможных отсрочек в 2–3 месяца.
- Формировать **точечный прогноз + доверительные интервалы**.
- Кластеризовать филиалы на группы **A/B/C** исходя из их особенностей и потенциала для прогнозирования.
- Сравнивать:
  - модельный прогноз,
  - ручные прогнозы менеджеров,
  - бейзлайн-модели.
- Обнаруживать потенциальные **кассовые разрывы**.
- Обеспечивать мониторинг качества и стабильности.

---

## 🧠 Бизнес-контекст

- Огромная распределенная по федерации филиальная сеть.
- Отгрузки и оплаты проходят с большой разницей во времени.
- Филиалы сильно различаются по плотности данных.
- Многие филиалы имеют короткую или разреженную историю.
- Прогнозы сейчас делаются вручную → непоследовательно, дорого, долго.
- Кассовые разрывы требуют планирования на основании прогноза.

---

## 🏗 Архитектура системы

Полная архитектурная схема (SVG) находится в  
**`docs/ts_forecast_architecture.svg`**
![Архитектура системы TS Forecast](docs/ts_forecast_architecture.svg)

---

## 📥 Входные данные (EDA)

Демо‑контур читает витрину EDA:

- `eda/data-mod/Поступления (по месячно).xlsx` — оплаты (`summa_vo_vv_month`)
- `eda/data-mod/Фактуры (по месячно).xlsx` — отгрузки (`stoimost_netto_month`)

## 🚀 Демо‑инференс (локально)

Быстрый запуск batch‑инференса:

```bash
python3 inference/run_inference.py
```

Запуск API для проверки результатов:

```bash
uvicorn inference.app:app --reload --port 8010
```

Веб‑интерфейс демо доступен по адресу `http://127.0.0.1:8010/`.

Результаты сохраняются в `outputs/inference/`:

- `forecast_next_month.csv`
- `quality_by_branch.csv`
- `mvp_vs_baseline.csv`
- `run_summary.json`

## 📊 Модули системы

### 1. **Data ingestion**

- Приём ежемесячных файлов выгрузки.
- (Потенциально) прямой доступ к ERP/DWH.
- Очистка и валидация данных.

### 2. **Feature engineering**

- Агрегация “филиал × месяц”.
- Лаги, rolling window, сезонность.
- Индикаторы активности и простоя.

### 3. **Классификация филиалов**

- **A** — устойчивые, качественный прогноз.
- **B** — средние, повышенная неопределённость.
- **C** — прогноз отсутствует (короткая история, разреженность).

### 4. **Модели**

- Глобальные модели для:
  - отгрузок,
  - оплат.
- Модели регрессии (CatBoost/LightGBM).
- Доверительные интервалы.

### 5. **API и UI**

- REST API (FastAPI)
- Лёгкий веб-интерфейс:
  - обзор сети,
  - графики прогнозов,
  - сравнение с ручным прогнозом и бейзлайном,
  - причины отбраковки.

### 6. **Мониторинг**

- MAPE, MAE, WAPE.
- Coverage доверительных интервалов.
- Рост доли C-филиалов.
- Состояние DAG-ов Airflow.
- Уведомления в Email + Telegram.

---

## 📁 Структура репозитория

```text
MLDesign/
├── eda/                         # EDA ноутбуки и артефакты данных (read-only)
├── baseline_forecast.ipynb      # Бейзлайны + backtesting + A/B/C + MVP
├── inference/                   # Демо‑инференс (batch + FastAPI)
├── outputs/inference/           # Артефакты инференса (CSV + summary)
├── docs/
│   ├── ml_system_design_doc.md  # Дизайн‑док (целевой продукт)
│   └── ts_forecast_architecture.svg
└── instr/                       # Тексты домашних заданий
```

## Установка зависимостей

### Python зависимости

```bash
pip install -r requirements-dev.txt
```

## Настройка pre-commit хуков

После установки зависимостей установите pre-commit хуки:

```bash
pre-commit install
```

Теперь при каждом коммите будут автоматически запускаться:

- Форматирование кода (Black)
- Сортировка импортов (isort)
- Линтинг (Ruff)
- Форматирование JSON, YAML, Markdown (Prettier)
- Проверка YAML, JSON, TOML файлов
- Удаление trailing whitespace
- Проверка на конфликты слияния
- И другие проверки

## Ручной запуск проверок

### Python

```bash
# Форматирование кода
black .

# Сортировка импортов
isort .

# Линтинг
ruff check .

# Линтинг с автоисправлением
ruff check . --fix

# Типизация
mypy .
```

### Форматирование конфигурационных файлов

```bash
# Форматирование JSON, YAML, Markdown
prettier --write "**/*.{json,yaml,yml,md}"
```

## Запуск всех pre-commit проверок вручную

```bash
pre-commit run --all-files
```
